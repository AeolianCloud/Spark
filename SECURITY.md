# 安全政策

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| master（最新） | ✅ 积极维护 |
| 已发布的正式版本（若有 tag/release） | ✅ 积极维护 |
| 更早的版本 | ❌ 不再维护 |

## 报告安全漏洞

**请勿通过公开的 GitHub Issue 报告安全漏洞。**

发现安全问题请通过私密渠道报告：

1. **首选**：GitHub Security Advisories（仓库 Security 页签 → Report a vulnerability）
2. **备选**：联系仓库维护者（GitHub Profile 中获取联系方式）

### 报告应包含

- 漏洞类型（如：注入、越权、加密缺陷、信息泄露等）
- 受影响文件路径与代码位置
- 复现步骤
- 潜在影响评估
- 修复建议（如有）

### 响应时间

| 阶段 | 时间 |
| --- | --- |
| 确认收到 | 48 小时内 |
| 初步评估 | 7 天内 |
| 修复发布 | 视严重程度，通常 30 天内 |

## 负责任披露

- 收到报告后尽快确认并评估，修复完成前不公开漏洞详情；
- 修复发布后在 Release Notes 中致谢报告者（除非要求匿名）。

## 已知安全现状与部署要求

当前 v1 版本**无认证鉴权**（见 README「已知限制」），以下措施为部署前提：

- **网络隔离**：API 与 Web 管理界面不得直接暴露公网，需部署在受控内网，或经反向代理（nginx 等）+ 网关鉴权（如 HTTP Basic / OAuth2 Proxy）后才可对外；
- **HTTPS**：经反向代理对外时启用 TLS；
- **密钥管理**：
  - `SPARK_CRYPTO_ENCRYPTION_KEY` 必须替换 `config/config.yaml` 中的示例值（启动时检测到示例值会打印警告），建议 `openssl rand -base64 32` 生成；
  - 本地敏感配置放入 `.env.local`（已 gitignore，不入库）；
  - 节点 API 令牌目前仍明文落库（见 docs/adr/0004，加密待实现），务必保证数据库访问控制严格；
- **最小权限**：PVE 节点 API token 使用 `PVEVMAdmin` 等限定角色，并在 PVE 侧限定资源池 / 节点范围（ACL 指派）；
- **敏感信息**：VM 密码经 crypto 包（AES-256-GCM，随机 nonce）加密后落库；错误消息对外脱敏，日志中不得出现明文密码 / token。

## 致谢

感谢所有帮助提升 Spark 安全性的研究者与开发者。
