# build artifacts
/spark
*.test

# runtime config (contains secrets; use config/config.yaml as template)
/config.local.yaml
config.local.yaml
/config/config.local.yaml

# local secrets (loaded via `set -a; source .env.local; set +a`)
.env.local

# editor/OS
.idea/
.vscode/
*.swp
.DS_Store
