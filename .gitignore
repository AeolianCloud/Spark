# build artifacts
/spark
*.test

# runtime config (contains secrets; use config/config.yaml as template)
/config.local.yaml
config.local.yaml
/config/config.local.yaml

# local secrets (auto-loaded by config.Load at startup)
.env.local

# editor/OS
.idea/
.vscode/
*.swp
.DS_Store
