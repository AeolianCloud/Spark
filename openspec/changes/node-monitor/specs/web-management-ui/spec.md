## ADDED Requirements

### Requirement: 节点列表简略状态

系统 SHALL 在节点总览页的节点表格中展示每个节点的简略实时状态：CPU 使用率与内存使用率（百分比）。状态数据 MUST 经 `GET /nodes/:id/status` 实时拉取；PVE 不可达的节点 MUST 展示降级提示（如"不可达"）而非伪造状态值。列表页 MUST 提供手动刷新能力，MUST NOT 以高于 10 秒的频率自动轮询。

#### Scenario: 列表展示简略状态

- **WHEN** 运维人员打开节点总览页
- **THEN** 每个节点行展示 CPU 与内存使用率，且值来自各节点的状态 API 实时响应

#### Scenario: 节点不可达降级

- **WHEN** 某节点的 PVE 不可达
- **THEN** 该节点行的状态展示为降级提示，并附错误说明，不影响其他节点行展示

#### Scenario: 手动刷新状态

- **WHEN** 运维人员点击刷新按钮
- **THEN** 界面重新请求各节点状态并展示最新值

### Requirement: 节点详情页

系统 SHALL 提供节点详情独立路由页（`/zones/:zoneId/nodes/:nodeId`），展示该节点的完整配置信息（名称、PVE 节点名、主机、端口、API 用户、启用状态）与实时运行状态：CPU（核数、使用率、负载）、内存（总量、已用、可用、使用率）、磁盘（根分区总量、已用、使用率）、网络（接口列表）、集群信息（PVE 版本、内核、在线时长、在线状态）。详情页 MUST 提供手动刷新能力，PVE 不可达时 MUST 展示降级提示而非伪造状态。

#### Scenario: 进入详情页

- **WHEN** 运维人员在节点列表点击某节点进入详情
- **THEN** 页面展示该节点的配置信息与实时状态，状态值来自 `GET /nodes/:id/status` 实时响应

#### Scenario: 详情页刷新

- **WHEN** 运维人员点击刷新按钮
- **THEN** 页面重新请求节点状态并展示最新值

#### Scenario: 详情页状态降级

- **WHEN** 该节点的 PVE 不可达
- **THEN** 详情页展示后端返回的降级错误信息，不展示任何伪造的状态值
